# Meridian：知识库使用与维护

项目工程约定、目录归属、验证和部署规则见 `CLAUDE.md`；开始代码工作前阅读相关部分。知识库维护格式见 `docs/knowledge/README.md`，历史检索入口为 `docs/knowledge/INDEX.md`。不把整个图谱一次塞入上下文，按任务阅读相关节点与证据。

## 什么时候使用

- 开始有不确定性的探索、搜索调研、原型设计、方案选择、架构调整或功能融合前，检索相关旧尝试、实验、经验和机制。明确本轮相对历史改变了什么、希望获得什么新信息。
- 遇到失败、反常结果、重复改动没有新信号，或准备扩大实验时，再查相似失败机制与适用条件，选择分析、调研、修改假设、复测、扩大验证、暂缓或停止。
- 普通格式修改、已明确的机械修复、简单解释不强制查库；是否属于探索以实际任务判断，不依据某个关键词机械触发。

## 什么时候写入

- 完成一轮有明确假设的实验或分析后，保存真实条件、结果、正常对照、证据位置与成本，关联对应尝试；没有新认识时可复用原经验，追加实验而非制造新结论。一次 LLM 调用不是一个必须入图的实验，原始输出留在日志或产物中。
- 调研形成会改变后续选择的依据、发现有效机制或失败边界、做出采用/暂缓/淘汰/融合决定时，写入相应经验、机制或决定。文献、会话推演与本仓实测分开，未执行的方案标 proposed，未知条件明确写未知。
- 阶段未结束但已出现重要信号时可以立即写入；探索停止或被中断时保留已有证据与未完成事项，不为补记录再调用 LLM。
- goal / attempt / experiment / lesson / mechanism / decision 按实际需要建实体，不要求每轮凑齐六类。保留具体实验历史，小样本通过与扩大样本失败分别记录。证据可解析、结构通过或被测模型自报通过都不是独立语义验收。

## 什么时候整理

- 探索阶段结束、准备融合或交接时，按相关任务整理；检索中发现重复、矛盾、过时或范围不清时，局部修正。
- 可以合并重复总结、补充关系和收窄边界，但不要删除或合并掉不同条件下的实验事实。旧决定/经验被取代时保留历史与替代关系，不因一次失败全盘否定方向。
- 不每轮重写整个知识库，不强迫做完原型就融合。搜索调研、读代码、原型、实验、假设调整和融合可以交错；只在证据支持时选择融合，融合后另验组合效果。

## 维护与每轮结束检查

- 只维护 `docs/knowledge/nodes/*.md`，不要手改 `INDEX.md` 或 `graph.json`；节点或 schema 改动后运行 `pnpm -s knowledge` 重建产物，再运行 `pnpm -s knowledge --check-generated` 检查结构与生成文件一致性。
- `.codex/hooks.json` 的 Stop hook 在每次回复结束时比较内容指纹；首次运行或节点、生成文件、校验规则变化时执行上述只读校验，其余静默跳过。仅缓存成功校验，不改写知识库。它不能判断是否遗漏了值得入库的经验，也不能证明结论正确；这些判断由 agent 根据本轮证据负责。
- 如果 hook 提示结构错误或索引过期，修复对应节点或重新生成，不靠削弱校验通过。再次结束仍失败时 hook 不无限续跑；在最终回复中明确未解决项和原因。
- 探索工作结束时简短交代新增/更新了哪些知识或为何无需写入；普通小改动无需额外知识库汇报。
