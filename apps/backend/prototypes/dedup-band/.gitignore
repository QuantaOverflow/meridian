node_modules/
__pycache__/
.cache/

# 运行产物一律进 out/，一次性脚本一律进 scratch/。
# 这样挡产物只需要两行，不用每轮追着新文件名补规则——
# 2026-09-05 之前是按文件名模式挡（*-result.json 之类），脚本却都往根目录写，
# 于是每轮都有漏网（armB-* / armC-* / armFa-* 等 70 个文件从没被挡住）。
out/
scratch/

# 输入 fixtures：可复现依赖，但含 384 维 embedding，两个窗口就 11MB。
# 暂不入库——重建方式见 README（gold/build-titles.sh 导 titles + DB 取 embedding）。
fixtures/
