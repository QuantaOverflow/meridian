# Session Handoff — 2026-05-28

## Resume From

Long session building Meridian's **observability + LLM-eval infrastructure**. Just
fixed **bug #1** (intel reports emitted "Fact 1/Fact 2/Entity 1" placeholders) — fix is
**deployed to ai-worker (Version 8e245ae5) but NOT committed**. Verification confirmed
placeholders gone, but the spot-checked story's intel came back empty — needs a look
(thin data vs `/stories/:id` mapping). Pick up by committing the fix, then confirming
intel content is actually rich now.

Read in order:
1. `.claude/pain-log.md` — running list of observed issues (story validation rate, bug #1, ml-service cold start)
2. `docs/engineering-notes/llm-pipeline-pitfalls.md` — prior migration gotchas
3. `scripts/eval/faithfulness/score.ts` — the eval tool entry (how to run, what it measures)
4. CLAUDE.md — project guide (commands, deploy, known gotchas)

## The arc (why this work exists)

User's core pain: **"我不知道好不好"** — no way to tell if a generated brief is good.
Vision crystallized as 3 layers: 可信(trustworthy) / 可诊断(diagnosable) / 可进化(improvable).
This session built the substrate for all three. Strategic call made: **upstream data
quality is the main thrust, fabrication-clamping is secondary** — "编造是饿出来的不是坏
出来的" (the brief fabricates because upstream gives it nothing real to say; fix the
vacuum, not just the symptom). Bug #1 is the first upstream fix.

## Environment

- Deploy: `cd <service> && pnpm exec wrangler deploy`. If "non-interactive environment /
  CLOUDFLARE_API_TOKEN" error → OAuth expired, user runs `pnpm exec wrangler login`.
  Deploy services **serially** (parallel triggers an auth race).
- **Surge proxy**: `api.cloudflare.com`→Proxies (added this session, needed for deploy),
  `workers.dev`→Proxies, `cloudflare.com`→DIRECT (keep — Docker registry needs it).
  Symptom of breakage: `fetch failed` on deploy / eval. Test: `curl api.cloudflare.com`.
- Run faithfulness eval: `cd scripts/eval/faithfulness && node_modules/.bin/tsx score.ts <workflow_id>`
  (deps installed via `pnpm install --ignore-workspace`; judge = qwen-max via ai-worker /meridian/chat).
- Trigger a brief: `curl -X POST https://meridian-backend.swj299792458.workers.dev/admin/briefs/generate -H "Content-Type: application/json" -d '{"timeRangeDays":3,"minImportance":2,"storyMinImportance":0.05}'` (loose params force stories to validate; default params often TERMINATED_NO_STORIES on thin days).

## Recent Commits

```
111a12f split factual vs analytical claims in faithfulness checker
577e7dd stop brief_generation fabricating from previous-day context
df63af4 add brief faithfulness eval checker
06acbd6 capture raw LLM input/output to R2 for eval
a0ca4b3 add 4 observability aggregation endpoints   <- origin/meridian-dev is HERE
8f713c5 propagate x-trace-id across backend / ai-worker / ml-service
575d561 add brief_runs / brief_stories / cluster_rejections tables
6786067 persist observability metrics on every step transition
```

**4 commits unpushed** (06acbd6, df63af4, 577e7dd, 111a12f). `git push` when ready.

## Uncommitted Changes

- `services/meridian-ai-worker/src/utils/intelligence-report-builder.ts` — **bug #1 fix,
  done + deployed, needs commit**. Changes: `buildEntities` reads `keyEntities.list`
  (was `entities`); `extractFactualBasis` derives from `timeline` (was hardcoded
  "Fact 1/Fact 2"); deleted orphaned `createDefaultEntities`.
- `scripts/eval/story-validation/` (untracked) — **do NOT commit** (user decided earlier).

## Key Files

| File | Role | Mark |
|---|---|---|
| services/meridian-ai-worker/src/utils/intelligence-report-builder.ts | bug #1 fix | ★ commit this |
| services/meridian-ai-worker/src/prompts/intelligenceAnalysis.ts | intel output schema (keyEntities.list, timeline, no factualBasis) — the contract source | reference |
| services/meridian-ai-worker/src/prompts/briefGeneration.ts | fabrication guardrail (rule 0 + previous-context lockdown) — committed | |
| services/meridian-ai-worker/src/services/llm-call-logger.ts | captures raw LLM I/O to R2 llm-calls/{trace}/{phase}-{idx}.json | |
| scripts/eval/faithfulness/ | the faithfulness checker (claims/judge/score/llm/types) | ★ eval tool |
| apps/backend/src/routers/observability.ts | query endpoints (/runs/:id, /runs/:id/llm-calls, /llm-calls/*, /trends, /health/summary) | |
| apps/backend/src/workflows/auto-brief-generation.ts | brief workflow: DB persistence + trace_id + x-call-index | |

## Decisions Made This Session (not visible from code)

- **Hybrid storage: DB metadata + R2 raw bodies** — Why: SQL-queryable trends + no row bloat — Rejected: pure-DB (bloat), pure-R2 (no trend queries).
- **Faithfulness = forced-evidence classification, not LLM numeric score** — Why: LLM 0-1 scores are uncalibrated noise; requiring a verbatim source quote (substring-verified) stops the judge faking "supported" — Rejected: judge confidence float.
- **Split factual vs analytical claims; only factual held to grounding** — Why: the brief's analytical voice is wanted, shouldn't be penalized as "unsupported"; headline = factual_faithfulness — analytical gets lighter consistency check.
- **Upstream data quality BEFORE analytical-fabrication clamp** — Why: clamping into a data vacuum yields honest-but-empty briefs; fix the vacuum first so analysis has real material — Rejected: prompt-only fabrication fix as the primary lever.
- **Bug #1 fixed in builder, not prompt** — Why: align consumer to producer's actual output (lower risk than changing LLM behavior) — root cause was prompt↔builder field-name contract drift.

## Known State / Data

- Faithfulness on `admin-brief-1779949426515`: **0.733 factual** (11/15 grounded), 0 contradicted, but **3 analytical claims built on fabricated premises** (analysis spun from previous-day identifiers — the fabrication migrated from fake-facts to fake-analysis after the briefGeneration fix).
- Bug #1 verification: `admin-brief-1779953505209` → placeholders gone (Fact1=False, Entity1=False) ✓, but `/stories/1/intel` returned empty overview/key_developments/stakeholders — **open question: thin data for that story, or brief_stories.id≠the rich intel file?**
- LLM I/O capture is live: `llm-calls/{workflow_id}/{phase}-{idx}.json` in R2 (phases: story_validation, intelligence_analysis, brief_generation×2, tldr_generation).
- Deployed versions: ai-worker `8e245ae5`, backend `ce9c4a4a`, ml-service `e192b176`.

## What NOT To Do

- Don't commit `scripts/eval/story-validation/` (user decided not to).
- Don't change Surge `cloudflare.com`→Proxies wholesale — breaks Docker registry; only `api.cloudflare.com`→Proxies.
- Don't fully clamp analytical fabrication yet — wait until upstream data is rich.
- Don't deploy services in parallel — wrangler auth race.

## First Action

`git add services/meridian-ai-worker/src/utils/intelligence-report-builder.ts && git commit` the bug #1 fix (message: "fix intel report placeholder fields — read keyEntities.list + derive factualBasis from timeline"). Then run `cd scripts/eval/faithfulness && node_modules/.bin/tsx score.ts admin-brief-1779953505209`, and pull `/observability/runs/admin-brief-1779953505209/llm-calls` → fetch an `intelligence_analysis-*.json` raw body to confirm the intel content is now rich (real key_developments + stakeholders), resolving the empty-story-1 question.
