# Session Handoff — 2026-06-03

## Resume From

聚类质量优化 + brief 管线三处改进**已全部上线生产**(确定性剪枝 P 0.45→0.83 / category 选样过滤 / 情报报告 R2 卸载解除 maxStories 1MB 限)。剩**端到端验证**和**残留大簇融合**两件事未做。

先读(深度细节在记忆里,别重复挖):
1. 记忆 `clustering_eval_harness.md`(本会话主线全记录:eval 设计、prune 方案、部署、brief 选样实情)
2. `scripts/eval/clustering/`(eval 工具:recluster.ts / tune.ts 已加 `--prune/--ms/--emb-key`)

## Environment

- 本地跑 ml-service(eval 用,已停):`cd services/meridian-ml-service && API_TOKEN=dev-token-123 EMBEDDING_MODEL_NAME="$(pwd)/model-cache" HF_HUB_OFFLINE=1 ./.venv/bin/uvicorn src.main:app --port 8081`
- 跑 eval 脚本:`scripts/eval/story-validation/node_modules/.bin/tsx`(根目录 `tsx` 没装)
- Docker(容器部署用):`/usr/local/bin/docker`,不在默认 PATH;部署要 `wrangler login` 含 `containers:write`
- 系统 python 无 numpy;用 `services/meridian-ml-service/.venv/bin/python`

## Recent Commits

```
1a833a2 docs: maxStoriesToGenerate 1MB 坑已由情报报告 R2 卸载解决
81b38e2 fix(brief): 情报报告卸载 R2,解除 maxStoriesToGenerate 的 1MB step 上限
97c1474 feat(brief): 自动选样按 category='news' 过滤,挡技术单篇噪音
9a0a7dc feat(clustering): 生产启用质心剪枝后处理(prune=0.92)
0e8887b feat(clustering): 确定性后处理(质心剪枝)+ eval 交叉验证锁阈值
```

## 已部署生产版本

- ml-service `24ff703a`(质心剪枝后处理代码)
- backend `db119207`(prune=0.92 下发 + category='news' 选样 + 情报报告 R2 卸载)

## Key Files

| File | Role | Mark |
|---|---|---|
| services/meridian-ml-service/src/clustering.py | `postprocess_labels`(质心剪枝/解散) | 核心算法 |
| services/meridian-ml-service/src/pipeline.py | ClusteringStage 接入后处理 | |
| apps/backend/src/lib/services/clustering.ts | 下发 `postprocess_prune_threshold=0.92` | 阈值与嵌入绑定 |
| apps/backend/src/workflows/auto-brief-generation.ts | category 选样过滤 + 情报报告 R2 卸载 | |
| scripts/eval/clustering/{recluster,tune}.ts | B-cubed eval + 扫参 | ★ 调参/验证入口 |

## Decisions Made This Session

- **prune=0.92 而非改 HDBSCAN 参数** — Why: 加 prune 后 prod 现有 mcs5/ms3 反而 F 最高(0.844),最小改动 — Rejected: mcs12/ms15(P 更高但 R 掉到 0.73,过度剪)
- **不建 LLM 兜底(§3)** — Why: 确定性剪枝单独已达 P≥0.75,YAGNI — 触发条件:大簇融合(见下)若观察到影响 brief 再做
- **e5 query: 前缀没加** — Why: 控制变量实测前缀只 +0.01 P(不是之前外推的 +0.08,旧缓存被文本差异混淆) — 不值得重嵌全库
- **阈值绑定嵌入分布** — prod 无前缀→0.92;若日后加前缀须用 tune.ts 重标

## Known State / Data

- 冻结金样本:`eval-reports/clustering/`(gitignored)4 天 reference-news-{05-29..06-01} + 合池,N=5 average-linkage consensus
- prune=0.92 实测:合池 P=0.729/R=0.92;跨 2 天窗口 meanP 0.83(各窗 0.96/0.98/0.97),确定性
- **残留问题(§3 触发条件)**:top-N 大簇里**最大那个仍有真故事融合**——cluster 23(38篇)=主故事 g2 63% + 另 2 个真故事;其余 top 簇 79-100% 纯。这是 C(融合),prune 治不了,要 LLM 拆分
- brief 选样:无 cron,手动 `POST /admin/briefs/generate`;默认 timeRangeDays=1/articleLimit=500/maxStories=15/minImportance=3。认证 token = 前端 `.env` 的 `NUXT_WORKER_API_TOKEN`(= backend API_TOKEN)

## What NOT To Do

- 别重嵌全库加 e5 前缀(实测只 +0.01,不值)
- 别从 root 部署;ml-service 部署要 Docker + `containers:write` scope
- 别动 `eval-reports/clustering/` 里 frozen:true 的 gold(冻结金样本)
- 阈值别套到不同嵌入上(0.92 是 prod 无前缀专用)

## First Action

跑 deploy→eval 的后半"eval A/B":对一份**部署前**的旧 brief 与**新触发**的一份 brief 分别跑 `scripts/eval/faithfulness`(传 workflowId)+ `scripts/eval/story-validation`,看 story-validation 拼盘检出率下降、faithfulness 编造关联减少。⚠️ 会花 DashScope LLM 钱(judge=qwen-max,走已 rotate 的新 gateway token);触发新 brief 用 `POST /admin/briefs/generate`(认证 token 见上)。先跟用户确认再花钱。

> 备注:secret 清理是更早的独立工作线,已完成(见记忆 `secret-remediation.md`),与本线无关。旧 handoff `docs/2026-05-28-handoff.md` 是另一主题(brief 触发),未删。
