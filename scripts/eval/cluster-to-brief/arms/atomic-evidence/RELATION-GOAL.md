# 关系核验因素细分探针

2026-09-17。仅 dev；不读取 heldout、不融合生产。

历史：practice-v1 中整句漏错 3/29、代码槽 7/29；lesson-practice-typed-anchors 指出词出现不等于角色/作用域/方向一致。当前不能把失败归因于纯架构或模型上限。

唯一新信息：保持模型和证据不变，加强比较指令，或强制显式关系配对，是否能改变已有漏判，同时保留正常对照？

材料预选：p08/p11/p12/p14/p20/p21/p29 的正常及错误句，共14题，按文本 hash 混排、opaque ID。它们是已知失败诊断材料，不是独立验证，不报告显著性。

三臂：replay 使用原整句接口；contrast 仅增加通用关系比较指令，输出契约不变；bindings 同指令但输出配对关系及每项比较，任一 unsupported/uncertain 拒绝父句。无需先定位风险，不使用人工关系表或错误标签。bindings 与 contrast 同时改变输出表示和输出计算量，不能严格归因于表示本身。

每批2题，三臂同批次；最多21批，各至多两次契约尝试，基础设施最终失败停止。新 relation-v1 输出目录；不进行语义重试、不根据中途结果改 prompt。复用本版缓存不算复测。标签只在全部模型请求后读取。

判定：分别报告错误父句放行、正常语义拒绝、契约失败、成本；本地 Codex 阅读配对输出，区分抽取错、比较错、最终决策不一致。父句拒绝不冒充逐错误正确诊断。

停止：本轮只做14题因素诊断，不自动扩大；若有信号，再选扩展与独立验证。模型能力没有通过更强被测模型对照，不能断言上限。
