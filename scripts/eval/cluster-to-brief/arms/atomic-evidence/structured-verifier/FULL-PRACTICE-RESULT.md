# 漏错优先：完整练习集缓存回放与工作点

2026-09-18。复用60题旧真实整句基线与4题真实转换的已复核关系产物，不是将当前分解原型重新跑60题LLM；没有新远程调用、heldout、独立验证、生产融合或部署。

## 新增代码与评测边界

- 对全部60题输入应用连续`数量+状态+单位`提取，绑定原文偏移。只支持areas/elements的受损/毁坏状态；不固定562、人名、簇号或题号。数字由精确decimal处理，近似/界限/否定/条件/冲突绑定/非法数字待复核。
- 必须绑定到同一个数量和单位；另一个句子出现destroyed不能把562 damaged areas改成562 completely destroyed areas。状态差异先待复核，不按精确引用自动判错，更不据此断言全球事实相反。
- 主Codex本地非盲核对原UNESCO报告、寺庙区域范围和数量绑定，确认p20-u差异未被证据支持、p20-s不触发拦截。审核绑定routing/hash及quantity/hash；未知群体身份和一般状态包含关系仍需复核。
- 所有模型/代码路由只使用inputs和已保存模型结果；参考labels在路由后用于逐错误记账。p18-u与p26-u的两处错误分别保留。旧整句拦截移除全部原错误，但不计为逐错误机制诊断。

## 完整32错误点，不静默排除失败

|原错误点结局|旧整句基线|加已复核关系/数量补丁|
|---|---|---|
|仍被放行|3|0|
|由旧整句判断挡住|28|28|
|由新增窄诊断挡住|0|3|
|旧接口失败待复核|1|1|

新增三诊断对应之前已观察漏点：p11归因、p12警告/完成态、p20数量/状态。其他28点仍只是旧整句阻断，不能宣称新原型独立诊断31/32。单独分解模块的完整覆盖仍未建立。

旧报告3/29来自两旧路线共同有效54题；本次完整分母是32，其中旧基线有效错误31、接口失败1。不能将3/29直接和0/32当同分母准确率比较。这里是针对已知错误的缓存修补回放，不能报告新版本总体漏错率0%、可靠性或模型能力提升。

## 两个工作点

|策略|放行/60|拦截/60|复核/60|原错误漏放|正常语义误拦|
|---|---|---|---|---|---|
|旧基线放行+已复核窄补丁|29|29|2|0，另1错误待复核|0/30，另1正常接口失败待复核|
|完整结构覆盖才放行|0|29|31|0，另1错误待复核|0/30，全部30正常需复核|

第一策略29放行全部来自旧基线，其他未覆盖风险依赖旧模型，其新材料漏错风险未知。2复核为旧接口失败p01-u和p16-s，不被包装成成功检错。第二策略复核51.7%、无人自动放行，低漏放是拒绝放行的结果，不能当核心能力改进。第一策略复核3.3%只是这个回放的工作量，不是可泛化的产品成本。

保留三态，但必须区分“没有漏放”“明确检出”和“放行收益”。当前值得继续验证的是旧基线+绑定补丁的候选工作点，尚不采用或部署；先冻结规则，在未参与修补的新开发事件上做有限真实实验，才能检查漏放与误拦，而不是继续对这三个已知漏点调提示。

## 证据与验证

`out/atomic-evidence/full-practice-v0.14/{routing,quantity-audit,scores}.json`；routing绑定输入/证据/旧基线内容hash，数量审核绑定整个routing及数量结果hash，结构审核重跑既有hash门。未改变旧输出或旧错误历史。

```
node scripts/eval/cluster-to-brief/arms/atomic-evidence/structured-verifier/full-practice-replay.mjs
node --test scripts/eval/cluster-to-brief/arms/atomic-evidence/structured-verifier/*.test.mjs
```

87回归通过，包含连续绑定与词命中负例、条件/界限/签名错误数字、语义审核过期、双错误父句逐项保留、参考risk不参与路由、无人放行不能伪装低漏错。typecheck4缓存任务通过，不替代mjs运行。新增远程调用/美元支出0；继承模型成本见历史记录，不重计。正常整句完整语义核验仍未通过。
