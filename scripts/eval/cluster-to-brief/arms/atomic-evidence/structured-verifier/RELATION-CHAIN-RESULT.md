# 真实转换接入关系比较：已定位旧错误，正常配对仍有缺口

2026-09-17。复用v0.9真实抽取，由v0.9.1确定性处理器生成角色/命题。只测试p11/p12两个旧错误及两个正常对照，无生产、heldout、远程judge。

本轮相对历史的新职责：身份消歧和命题配对分离；命题配对不见说话人、报告类型或状态字段；最终关系差异由代码生成。希望了解修复抽取后能否真正定位旧错误、以及正常关系是否保留，而非继续统计接口通过。

## 冻结条件和失败历史

每轮上限7逻辑/14 HTTP/15000已知tokens、60秒单请求，固定Workers AI REST/glm-4.7-flash；最多一次实际校验错误反馈，不扩样。既有10合法报告act/declined义务保留，不重新抽取。

|轮次|变化|真实成本|结果|
|---|---|---|---|
|v0.10|1次He消歧+6次单命题配对，模型复制引用ID/锚点|8HTTP/4143tokens/累计请求35.469秒|He两次编造ID；1个正常“披露影响”错配到“公告措辞”，锚点合法仍语义错。初始候选ID含开发编号，非盲，不作泛化证据|
|v0.11|编号选择代替ID复制、代码写回引用；候选ID不透明；提示区别公告措辞/披露后果|9HTTP/3508tokens/累计请求34.238秒|He→Meink有原文依据；5/6命题提出忠实配对，正常p12返回0未知。两次自然引用校验失败反馈后恢复，仍不把格式修复计语义通过|

合计17 HTTP/7651已知tokens，无连接失败；累计请求秒数非墙钟/金额，美元未知。v0.11含针对已观察事件混淆的开发提示，不是独立或盲验证，也不是纯选择字段消融。所有请求、原输出、失败和条件分别留在`out/atomic-evidence/relation-chain-v0.{10,11}/`。

## 本地复核后的代码结果

主Codex非盲对照原文复核5个配对及1个代词解析。模型只做自然语言结构转换；没有调用远程judge。`local-audit.json`绑定完整原始结果hash、每个配对hash、解析hash。代码必须通过匹配的本地语义/身份复核才消费关系差异；未复核、被否或过期配对保持pending，不得凭引用合法判支持。

|目标|已观察结果|未完成|
|---|---|---|
|p11-u旧错误|两个Reporters→命题关系均与原始Meink→命题不一致|第二报告would时间未知，不伪造其时态冲突|
|p12-u旧错误|confirm与warn、completed与future分别不一致|并非对报道真实性的通用判断|
|p11-s正常|2报告配对正确；speaker/reportMode/polarity均一致，第1eventState一致|第2eventState未知，完整语义覆盖未证明，整句pending|
|p12-s正常|未被错误拒绝|配对返回未知，属于正常保留缺口，不能计正常核验成功|

两个已知错误病例得到针对原问题的诊断，不是只因接口失败被拒绝；不能称独立2/2可靠性，也不能把正常未知当正确拒绝或正确通过。

聚合只表示`not_supported_in_registered_evidence`或pending：已复核关系差异足以点名该关系未被注册证据支持；正常整句不自动pass，完整覆盖仍false。source declined未支持义务仍保留。一般别名身份、完整限定范围、数量族等未实现。

## 可复现与下一步

- `node --test scripts/eval/cluster-to-brief/arms/atomic-evidence/structured-verifier/*.test.mjs`：71测试通过，包括配对语义审查门、过期审查拒绝、引用负例、未知不当正确拒绝。`pnpm -s typecheck`4任务缓存通过，不替代mjs运行。
- `node scripts/eval/cluster-to-brief/arms/atomic-evidence/structured-verifier/relation-review.mjs`：复用已保存调用和本地audit，输出`reviewed-results.json`，新增远程调用0。
- 本轮到有限预算停止，不扩大融合。下一步应针对正常p12配对未知做事件/参与者结构拆分及正常对照，而不是放宽匹配、用主Codex答案填回模型输出或直接扩大样本。v0.10正常p12可配对而v0.11未知的历史保留，不能只展示改善。
